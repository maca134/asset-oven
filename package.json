{
	"name": "asset-oven-repo",
	"version": "0.1.0",
	"description": "Lazy, content-hashed asset middleware for Bun — local asset paths in server-rendered HTML just work.",
	"type": "module",
	"license": "MIT",
	"private": true,
	"scripts": {
		"lint": "eslint .",
		"format": "prettier --write ."
	},
	"workspaces": [
		"examples/*",
		"asset-oven"
	],
	"devDependencies": {
		"@eslint/js": "^10.0.1",
		"@types/bun": "latest",
		"eslint": "^10.7.0",
		"eslint-config-prettier": "^10.1.8",
		"prettier": "^3.9.5",
		"typescript-eslint": "^8.63.0"
	}
}
